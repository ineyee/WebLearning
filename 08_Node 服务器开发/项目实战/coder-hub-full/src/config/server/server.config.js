class ServerConfig {
  // 部署到服务器时记得换成服务器的域名或 IP 地址，还有端口号
  // static SERVER_HOST = "http://localhost"; // 本地
  static SERVER_HOST = "http://118.25.70.197"; // 远程
  static SERVER_PORT = 8000;
}

module.exports = ServerConfig;
